# Ollama default settings
# These are fallback values used when ~/.sai/config.toml doesn't exist
# Config file takes priority over environment variables

OLLAMA_URL=http://localhost:11434
OLLAMA_MODEL=llama3.2:1b